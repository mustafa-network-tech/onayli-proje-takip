import {db} from "@/lib/db";import Link from "next/link";export const dynamic="force-dynamic";export default async function Teams(){const teams=await db.team.findMany({include:{_count:{select:{entries:true}},entries:{select:{workDate:true}}},orderBy:{name:"asc"}});return <><div className="top"><div><h1>Ekipler</h1><div className="muted">Performans kaydı bulunan ekipler</div></div></div><div className="table-wrap"><table><thead><tr><th>Ekip</th><th>Aktif Gün</th><th>İmalat Kaydı</th></tr></thead><tbody>{teams.map(t=><tr key={t.id}><td><Link href={`/performance/teams/${t.id}`}><b>{t.name}</b></Link></td><td>{new Set(t.entries.map(e=>e.workDate.toISOString().slice(0,10))).size}</td><td>{t._count.entries}</td></tr>)}</tbody></table></div></>}
