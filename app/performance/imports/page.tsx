import {db} from "@/lib/db";import ProductionDeleteButton from "@/components/ProductionDeleteButton";export const dynamic="force-dynamic";export default async function Imports(){const items=await db.productionImport.findMany({include:{user:true},orderBy:{uploadedAt:"desc"}});return <><div className="top"><div><h1>Yüklenen PDF'ler</h1><div className="muted">Performans import geçmişi</div></div></div><div className="table-wrap"><table><thead><tr><th>Dosya</th><th>Tarih</th><th>Yükleme</th><th>Ekip</th><th>İmalat</th><th>Tanınmayan</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.fileName}</td><td>{i.workDate.toLocaleDateString("tr-TR")}</td><td>{i.uploadedAt.toLocaleString("tr-TR")}</td><td>{i.totalTeams}</td><td>{i.totalItems}</td><td>{i.unknownItems}</td><td>{i.status}</td><td><ProductionDeleteButton id={i.id} fileName={i.fileName}/></td></tr>)}</tbody></table></div></>}
