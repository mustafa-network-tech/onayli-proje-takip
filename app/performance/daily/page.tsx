import {db} from "@/lib/db";import ProductionSummary from "@/components/ProductionSummary";export const dynamic="force-dynamic";const iso=(d:Date)=>d.toISOString().slice(0,10);export default async function Daily({searchParams}:{searchParams:Promise<{date?:string}>}){const q=await searchParams,date=q.date??iso(new Date()),start=new Date(`${date}T00:00:00Z`),end=new Date(`${date}T23:59:59.999Z`),teams=await db.team.findMany({where:{entries:{some:{workDate:{gte:start,lte:end}}}},include:{entries:{where:{workDate:{gte:start,lte:end}}}},orderBy:{name:"asc"}});return <><div className="top"><div><h1>Günlük Performans</h1><div className="muted">Ekiplerin günlük gerçek üretim miktarları</div></div></div><form className="filters"><input type="date" name="date" defaultValue={date}/><button>Göster</button></form><div className="grid">{teams.map(t=><div className="card" key={t.id}><h2>{t.name}</h2><ProductionSummary entries={t.entries}/></div>)}</div>{!teams.length&&<div className="card">Bu tarihte performans kaydı yok.</div>}</>}
