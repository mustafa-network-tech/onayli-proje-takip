import {db} from "@/lib/db";import ProductionSummary from "@/components/ProductionSummary";import Link from "next/link";export const dynamic="force-dynamic";export default async function Monthly({searchParams}:{searchParams:Promise<{month?:string}>}){const now=new Date(),month=(await searchParams).month??now.toISOString().slice(0,7),[y,m]=month.split("-").map(Number),start=new Date(Date.UTC(y,m-1,1)),end=new Date(Date.UTC(y,m,1)),teams=await db.team.findMany({where:{entries:{some:{workDate:{gte:start,lt:end}}}},include:{entries:{where:{workDate:{gte:start,lt:end}}}},orderBy:{name:"asc"}});return <><div className="top"><div><h1>Aylık Performans</h1><div className="muted">Ekip ve imalat türü bazlı aylık toplamlar</div></div></div><form className="filters"><input type="month" name="month" defaultValue={month}/><button>Göster</button></form><div className="grid">{teams.map(t=><div className="card" key={t.id}><h2><Link href={`/performance/teams/${t.id}`}>{t.name}</Link></h2><p className="muted">Aktif gün: {new Set(t.entries.map(e=>iso(e.workDate))).size}</p><ProductionSummary entries={t.entries}/></div>)}</div></>};function iso(d:Date){return d.toISOString().slice(0,10)}
