import {describe,expect,it} from "vitest";import {parseProductionLine,parseProductionText} from "./production-parser";
describe("üretim satırı parserı",()=>{it.each(["KAZI (40*80) 55 METRE","150 METRE KAZI (40*80)"])("sıra bağımsız kazı: %s",line=>{const x=parseProductionLine(line,"Ekip");expect(x).toMatchObject({category:"KAZI",subtype:"40x80",unit:"METRE"})});it("FO-H varyasyonunu kapasiteyle normalize eder",()=>{expect(parseProductionLine("6 FO-H KABLO ÇEKİMİ 125 MT","Ekip")).toMatchObject({category:"KABLO ÇEKİMİ",cableFamily:"FOH",cableCapacity:6,quantity:125,unit:"METRE"})});it("adet işi ayırır",()=>{expect(parseProductionLine("3 ADET EK ODASI BULMA-YÜKSELTME","Ekip")).toMatchObject({category:"EK ODASI BULMA-YÜKSELTME",quantity:3,unit:"ADET"})});it("anlamsız satırı reddeder",()=>expect(parseProductionLine("xxz","Ekip")).toBeNull())});
describe("OCR boşluk kayıpları",()=>{it("bitişik FOH kablo satırını tanır",()=>expect(parseProductionLine("1: 6GFOHKABLOÇEKİMİ 125 METRE","Ekip")).toMatchObject({category:"KABLO ÇEKİMİ",cableFamily:"FOH",cableCapacity:6,quantity:125}));it("bitişik adet ve imalatı tanır",()=>expect(parseProductionLine("1: 1ADETEKODASIÖRME (KAPAK)","Ekip")).toMatchObject({category:"EK ODASI ÖRME",quantity:1,unit:"ADET"}))});
describe("PDF metni",()=>{it("notu yok sayar ve ekip/tarih çıkarır",()=>{const p=parseProductionText("Tarih: 26.08.2026\nEKİP ADI: Hamdullah Ertaş\nNOT: açıklama\n150 MT SURVEY\nxxz");expect(p.workDate).toBe("2026-08-26");expect(p.entries).toHaveLength(1);expect(p.ignored).toContain("NOT: açıklama");expect(p.unknown).toHaveLength(1)})});
