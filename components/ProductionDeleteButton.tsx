"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function ProductionDeleteButton({id,fileName}:{id:string;fileName:string}){const router=useRouter(),[busy,setBusy]=useState(false);async function remove(){if(!confirm(`"${fileName}" silindiğinde bu importtan oluşturulan performans kayıtları da silinecektir. Devam edilsin mi?`))return;setBusy(true);const r=await fetch(`/api/performance/imports/${id}`,{method:"DELETE"}),j=await r.json();r.ok?router.refresh():alert(j.error);setBusy(false)}return <button style={{background:"#a63d40"}} disabled={busy} onClick={remove}>{busy?"Siliniyor…":"PDF ve Kayıtları Sil"}</button>}
